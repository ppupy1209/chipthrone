# 이 파일을 .env로 복사한 뒤 값을 채운다. .env는 .gitignore에 있어 커밋되지 않는다.
#   cp .env.example .env
#
# 시세(Hyperliquid)와 원화 환산(업비트)은 인증키 없이 동작한다.

# 애플리케이션 장애와 배포 성공·실패·자동 롤백 알림. 비워 두면 Slack 알림을 보내지 않는다.
SLACK_WEBHOOK_URL=
