# 이 파일을 .env로 복사한 뒤 값을 채운다. .env는 .gitignore에 있어 커밋되지 않는다.
#   cp .env.example .env
#
# 환율(Pyth Network)은 키가 필요 없다. 아래 키가 비어 있으면 확정 종가·확정 시가총액·
# 마감 시점 괴리율만 비고, 나머지 화면은 그대로 동작한다.

# 공공데이터포털 "금융위원회_주식시세정보" 일반 인증키 (Decoding 쪽을 넣는다)
# https://www.data.go.kr
PUBLIC_DATA_SERVICE_KEY=
